# backend/.env
# app/core/config.py의 Settings 클래스와 필드 이름을 일치시킨 최종 버전

# === POSTGRES ===
POSTGRES_SERVER=db
POSTGRES_USER=r3user
POSTGRES_PASSWORD=r3password
POSTGRES_DB=r3db

# === JWT / Auth ===
# (JWT_SECRET_KEY -> SECRET_KEY 로 변경)
SECRET_KEY=a-very-long-and-secure-random-string-please-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# === Media / Storage ===
MEDIA_ROOT=/app/media